animations = ({
    triggers = ["open"];
    preset = "appear";
    scale = 0.92;
    duration = 0.22;
}, {
    triggers = ["close"];
    preset = "fly-out";
    direction = "left";
    duration = 0.20;
}, {
    # Show — cascading fly-in
    triggers = ["show"];
    preset = "fly-in";
    direction = "up";
    duration = 0.2;
}, {
    # Hide — cascading fly-out
    triggers = ["hide"];
    preset = "disappear";
    scale = 1.05;
    duration = 0.10;
});





animations = ({
    triggers = ["open"];
    preset = "appear";
    scale = 0.90;
    duration = 0.30;
}, {
    triggers = ["close"];
    preset = "fly-out";
    direction = "down";
    duration = 0.20;
}, {
    triggers = ["show"];
    preset = "fly-in";
    direction = "up";
    duration = 0.30;
}, {
    triggers = ["hide"];
    preset = "fly-out";
    direction = "down";
    duration = 0.25;
}, {
    triggers = ["workspace-in"];
    preset = "appear";
    scale = 0.95;
    duration = 0.25;
}, {
    triggers = ["workspace-out"];
    preset = "disappear";
    scale = 1.05;
    duration = 0.20;
}, {
    triggers = ["position"];
    preset = "geometry-change";
    duration = 0.30;
}, {
    triggers = ["size"];
    preset = "geometry-change";
    duration = 0.35;
});

